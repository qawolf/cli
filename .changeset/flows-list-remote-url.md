---
"@qawolf/cli": minor
---

`flows list --remote --json` now includes each flow's `url`, the flow page on the platform. The agent skill explains that a flow gets a platform page only once it is on the environment's flow code branch and how to land it there.
