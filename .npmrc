# @qawolf packages are published to public npm for this open-source CLI.
# A developer's global ~/.npmrc may redirect @qawolf to GitHub Packages
# (for the private platform repos); this overrides that back to public npm.
@qawolf:registry=https://registry.npmjs.org/
